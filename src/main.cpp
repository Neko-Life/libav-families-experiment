#include <cstdint>
#include <cstdio>
#include "opus/opus.h"

int main() {
  printf("Hello World!");

  int32_t samp_rate = 48000;
  int channels = 2;

  // init decoder
  /* int error; */
  /* OpusDecoder *dec; */
  /* dec = opus_decoder_create(samp_rate, channels, &error); */

  return 0;
}
